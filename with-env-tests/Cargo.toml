[package]
name = "with-env-tests"
version = "0.1.0"
edition = "2024"
authors = ["Rinat Balbekov <ribiens@gmail.com>"]
license = "MIT OR Apache-2.0"

[dependencies]
serde-with-env = { path = "../../serde-with-env"}
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1" }